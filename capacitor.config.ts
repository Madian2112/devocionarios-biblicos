import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devocionalesbiblicos.devocionales',
  appName: 'Devocionales',
  webDir: 'out'
};

export default config;
